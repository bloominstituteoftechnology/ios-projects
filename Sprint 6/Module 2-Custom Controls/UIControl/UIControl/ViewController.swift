import UIKit

class ViewController: UIViewController {
    
    @IBAction func updateRating(ratingControl: CustomControl) {
//  Implement `updateRating`. Set the view controller's title to the string
//  `"User Rating: N stars"` where N is the number of stars. This number is the   control's visible `value` property.
        
    }
    
    override func viewDidLoad() {
        super.viewDidLoad()

    }
    
    func addTarget(_ target: Any?, action: Selector, for controlEvents: UIControl.Event) {
        
    }
}

